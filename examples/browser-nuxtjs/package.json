{
  "name": "browser-nuxtjs",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal demonstration of how to use js-ipfs in a `create-nuxt-app` generated app.",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./.nuxt ./dist",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "test": "npm run generate && playwright test tests"
  },
  "dependencies": {
    "core-js": "^3.15.1",
    "ipfs": "^0.58.1",
    "nuxt": "^2.15.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.12.3",
    "playwright": "^1.12.3",
    "rimraf": "^3.0.2",
    "test-util-ipfs-example": "^1.0.2"
  },
  "workspaces": {
    "nohoist": ["**"]
  }
}
