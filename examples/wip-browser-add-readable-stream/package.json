{
  "name": "wip-example-browser-add-readable-stream",
  "description": "How to add readable streams in the browser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "parcel build public/index.html --dist-dir ./dist --no-scope-hoist",
    "serve": "parcel serve public/index.html",
    "start": "npm run serve",
    "test": "test-ipfs-example",
    "clean": "rimraf ./dist ./.cache ./.parcel-cache"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "parcel": "latest",
    "rimraf": "^3.0.2",
    "@playwright/test": "^1.12.3"
  },
  "dependencies": {
    "ipfs": "^0.55.4"
  }
}
